include includes/header
html(lang="en")
    head
        meta(charset="UTF-8")
        title= title
        script(src="/js/opretUser.js" defer)
        link(rel="stylesheet", href="/css/opretUser.css")
    body
        input(type='button' name='logud',,id='logud' value='Log Ud')
        div(id='wholeBody')
            h1="Opret Bruger"
            p="Her kan du oprette nye bruger til systemmet."
            input(type='text' name='brugernavn',, id='brugernavn' placeholder='brugernavn' required)
            input(type='text' name='password',, id='password' placeholder='password' required)
            input(type='text' name='brugerniveau',, id='brugerniveau' placeholder='brugerniveau' required)
            input(type='button' name='opretUser',, id='opretUser' value='Opret User')
            input(type='button' name='tilbage',, id='tilbage' value='Tilbage')
